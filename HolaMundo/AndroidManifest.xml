<?xml version="1.0" encoding="utf-8"?>
<!-- Presentation card of my application
     Describes what your application does and what permissions it has. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.holamundo"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="19" />
    <!-- Has permission to use internet -->
    <uses-permission android:name="android.permission.INTERNET"/>

     <!-- @drawable Reference to a value in res drawable -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <!-- Activity represents a window in Android -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
